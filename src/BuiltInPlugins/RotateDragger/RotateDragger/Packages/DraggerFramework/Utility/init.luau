return {
    Analytics = require(script.Analytics), 
    AnimationTask = require(script.AnimationTask), 
    AttachmentMover = require(script.AttachmentMover), 
    BoundingBox = require(script.BoundingBox), 
    BufferedSignal = require(script.BufferedSignal), 
    Colors = require(script.Colors), 
    CursorGroupName = require(script.CursorGroupName), 
    DebugFlags = require(script.DebugFlags), 
    DragHelper = require(script.DragHelper), 
    DragSelector = require(script.DragSelector), 
    HotkeyHelpBuilder = require(script.HotkeyHelpBuilder), 
    JointMaker = require(script.JointMaker), 
    JointPairs = require(script.JointPairs), 
    JointUtil = require(script.JointUtil), 
    Math = require(script.Math), 
    MockAnalytics = require(script.MockAnalytics), 
    PartMover = require(script.PartMover), 
    SelectionHelper = require(script.SelectionHelper), 
    SelectionWrapper = require(script.SelectionWrapper), 
    Signal = require(script.Signal), 
    SoftSnapper = require(script.SoftSnapper), 
    StandardCursor = require(script.StandardCursor), 
    TemporaryTransparency = require(script.TemporaryTransparency), 
    ViewChangeDetector = require(script.ViewChangeDetector), 
    assertGoodCFrame = require(script.assertGoodCFrame), 
    blackboxFindClosestMeshEdge = require(script.blackboxFindClosestMeshEdge), 
    classifyPivot = require(script.classifyPivot), 
    computeDraggedDistance = require(script.computeDraggedDistance), 
    computeHandlePositionDistance = require(script.computeHandlePositionDistance), 
    conciseNumberFormat = require(script.conciseNumberFormat), 
    fixSlightlyOffgridOrientation = require(script.fixSlightlyOffgridOrientation), 
    getBoundingBoxScale = require(script.getBoundingBoxScale), 
    getEdgeHotspot = require(script.getEdgeHotspot), 
    getFaceInstance = require(script.getFaceInstance), 
    getGeometry = require(script.getGeometry), 
    getSoftSnaps = require(script.getSoftSnaps), 
    isCli = require(script.isCli), 
    isFTF = require(script.isFTF), 
    isGoodRaycastResult = require(script.isGoodRaycastResult), 
    isProtectedInstance = require(script.isProtectedInstance), 
    roundRotation = require(script.roundRotation), 
    setInsertPoint = require(script.setInsertPoint), 
    shouldDragAsFace = require(script.shouldDragAsFace), 
    snapRotationToPrimaryDirection = require(script.snapRotationToPrimaryDirection)
};