return {
    DragSelecting = require(script.DragSelecting), 
    DraggingFaceInstance = require(script.DraggingFaceInstance), 
    DraggingHandle = require(script.DraggingHandle), 
    DraggingParts = require(script.DraggingParts), 
    PendingDraggingParts = require(script.PendingDraggingParts), 
    PendingSelectNext = require(script.PendingSelectNext), 
    Ready = require(script.Ready)
};