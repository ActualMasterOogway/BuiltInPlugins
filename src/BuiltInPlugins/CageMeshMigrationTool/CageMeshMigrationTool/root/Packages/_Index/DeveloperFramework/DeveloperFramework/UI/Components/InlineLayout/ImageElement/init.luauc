local Framework = script:FindFirstAncestor("UI").Parent

local InlineElement = require(Framework.UI.Components.InlineLayout.InlineElement)
local Image = require(Framework.UI.Components.Image)

local ImageElement = {}

function ImageElement.new(props: { [any]: any })
	return InlineElement.new(Image, props)
end

return ImageElement
