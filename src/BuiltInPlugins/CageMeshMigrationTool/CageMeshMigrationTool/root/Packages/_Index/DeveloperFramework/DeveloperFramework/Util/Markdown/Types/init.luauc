export type Token = {
	name: string,
	text: string,
	capture: { string }?,
	isBlock: boolean?,
	offset: number,
}
export type Attributes = { [string]: string | number | nil }
export type Node = {
	name: string,
	children: { Node },
	attributes: Attributes?,
	text: string?,
	isBlock: boolean?,
	url: string?,
	offset: number,
}

return {}
