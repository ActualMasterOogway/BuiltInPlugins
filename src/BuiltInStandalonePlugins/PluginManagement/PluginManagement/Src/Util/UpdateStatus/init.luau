local l_Parent_0 = script.Parent.Parent.Parent;
local v1 = require(l_Parent_0.Packages.Symbol);
return {
    Updating = v1.named("Updating"), 
    Success = v1.named("Success"), 
    Error = v1.named("Error")
};